/** Keep in sync with `src/renderer/cloud-sync/cloud-sync-storage.ts` key usage. */
export const ARCHON_SYNC_ACCESS_TOKEN_KEY = "archon-sync-auth-token";
export const ARCHON_SYNC_REFRESH_TOKEN_KEY = "archon-sync-refresh-token";

/** `sessionStorage` path+query to open after web sign-in (e.g. `/mcp-auth?user_code=…`). */
export const ARCHON_POST_AUTH_REDIRECT_KEY = "archon-post-auth-redirect";
