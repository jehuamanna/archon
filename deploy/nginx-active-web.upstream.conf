# Active web backend host:port — managed by scripts/docker-web-deploy.sh / docker-web-swap.sh
# Default matches the slot Compose starts first (archon-web-blue).
set $archon_web_backend "archon-web-blue:3000";
